# Optional. Without this key the app deliberately runs in deterministic Demo Mode.
OPENAI_API_KEY=

# The Build Week model. You may override this only if your OpenAI account requires it.
OPENAI_MODEL=gpt-5.6

# Default port for the local demo.
PORT=3000
